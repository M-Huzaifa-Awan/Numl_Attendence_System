@{
    string controllerName = this.ViewContext.RouteData.Values["controller"]?.ToString()+"";

    switch (controllerName)
    {
        case "Login":
            Layout = "_LayoutLogin";
            break;
        default:
            Layout = "_Layout";
            break;
    }
}